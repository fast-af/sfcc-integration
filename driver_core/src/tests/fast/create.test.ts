//import fastOrder from "../../core/index";
import { newOrder } from "../testdata";

function createTest() {
  test("Status check on order create", () => {
    //expect(fastOrder(newOrder, "create").status).toBe("200");
  });
  return;
}

createTest();
